import { SkeletonSettings } from '@/components/ui/skeleton';

/**
 * Admin settings loading skeleton — heading + 3 settings cards with form fields.
 */
export default function AdminLoading() {
    return <SkeletonSettings />;
}
